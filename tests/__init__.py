"""Test package.

Present so ``python -m unittest discover -s tests -t .`` can import the test
modules while the application modules (``app``, ``processor``, ...) stay
importable from the repository root.
"""
